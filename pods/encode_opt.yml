!ImageKerasEncoder
with:
  model_name: ${{JINA_MODEL_NAME_VAR}} # any model could go here
  pool_strategy: avg
  channel_axis: -1
requests:
  on:
    ControlRequest:
      - !ControlReqDriver {}
    [SearchRequest, IndexRequest]:
      - !EncodeDriver
        with:
          traversal_paths: ['r']
